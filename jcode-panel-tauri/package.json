{
  "name": "jcode-panel-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "vite --host 127.0.0.1 --port 1420",
    "build": "tauri build",
    "build:linux-packages": "tauri build --bundles deb,rpm",
    "build:web": "vite build",
    "build:full": "bash scripts/full-build.sh",
    "build:full:restart": "bash scripts/full-build.sh --restart",
    "check": "tsc --noEmit",
    "lint:structure": "node scripts/check-structure.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.0"
  }
}
